[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
log_cli = true
log_cli_level = INFO
addopts = --json-report --json-report-file=tests/test_report.json

markers =
    slow: marks tests as slow (deselect with -m 'not slow')
    integration: marks tests that require real external services (Keycloak, LiveKit, etc.)
    unit: marks fast unit tests using mocked dependencies
